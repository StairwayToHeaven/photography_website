<!DOCTYPE html>
<!--[if gt IE 8]><!--> <html class="no-js"> <!--<![endif]-->
    <head>
        <meta charset="utf-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <title>{% block title %} {% endblock %}Katarzyna Dam - portfolio</title>
        <meta name="description" content="">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <link href="{{ asset('css/styles.css') }}" rel="stylesheet">

        <link href="//maxcdn.bootstrapcdn.com/bootstrap/3.2.0/css/bootstrap.min.css" rel="stylesheet">
        <link href="//maxcdn.bootstrapcdn.com/bootswatch/3.2.0/darkly/bootstrap.min.css" rel="stylesheet">
		<!-- HTML5 shim and Respond.js for IE8 support of HTML5 elements and media queries -->
        <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
        <!--[if lt IE 9]>
        <script src="https://oss.maxcdn.com/html5shiv/3.7.3/html5shiv.min.js"></script>
        <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
        <![endif]-->
        <link href="{{ asset('css/styles.css') }}" rel="stylesheet">

    </head>
    <body>
		{% include 'default/_flash_messages.html.twig' %}
   		<div class="navbar navbar-inverse navbar-fixed-top" role="navigation">
	      <div class="container">
	        <div class="navbar-header">
	          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target=".navbar-collapse">
	            <span class="sr-only">Toggle navigation</span>
	            <span class="icon-bar"></span>
	            <span class="icon-bar"></span>
	            <span class="icon-bar"></span>
	          </button>
	          <a class="navbar-brand" href="{{ url('home') }}">WITAJ!</a>
	        </div>
	        <div class="collapse navbar-collapse">
	          <ul class="nav navbar-nav" >
	            <li{% if active == 'home' %} class="active"{% endif %}><a href="{{ url('home') }}">Home</a></li>
	            {{ render(url('page-menu', {'active': active})) }}
	            <li{% if active == 'portfolio' %} class="active"{% endif %}><a href="{{ url('portfolio') }}">Portfolio</a></li>
				{% if is_granted('IS_AUTHENTICATED_FULLY') %}
					<li{% if active == 'wylogowanie' %} class="active"{% endif %}><a href="{{ url('auth_logout') }}">Wylogowanie</a></li>
				{% else %}
					<li{% if active == 'logowanie' %} class="active"{% endif %}><a href="{{ url('auth_login') }}">Logowanie</a></li>
					<li{% if active == 'rejestracja' %} class="active"{% endif %}><a href="{{ url('user-add') }}">Rejestracja</a></li>
				{% endif %}
				{% if is_granted('ROLE_ADMIN') %}
					<li{% if active == 'admin' %} class="active"{% endif %}><a href="{{ url('user-index', {page: 1}) }}">Admin</a></li>
				{% endif %}
				{% if is_granted('ROLE_USER') %}
					<li{% if active == 'profil' %} class="active"{% endif %}><a href="{{ url('user-view') }}">Profil</a></li>
				{% endif %}
				{% if is_granted('ROLE_ADMIN') %}
					<li{% if active == 'page-add' %} class="active"{% endif %}><a href="{{ url('page-add') }}">+</a></li>
				{% endif %}
	          </ul>
	        </div>
	      </div>
	    </div>

	    <div class="container">
			{% block content %}

			{% endblock %}
	    </div><!-- /.container -->
       	

		<script src="//ajax.googleapis.com/ajax/libs/jquery/1.10.2/jquery.min.js"></script>
		<script src="//maxcdn.bootstrapcdn.com/bootstrap/3.2.0/js/bootstrap.min.js"></script>
    </body>
</html>
